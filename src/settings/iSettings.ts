export default interface Settings {
  dotfiles: string
  rootfile: string
  localfile: string
  outfile?: string
  playbook: string
  provisionDir: string
  deployScript: string
  updateScript: string
}
